# Environment variables for signaling server

# Server
PORT=8080
HOST=0.0.0.0
NODE_ENV=development

# Logging
LOG_LEVEL=info

# CORS
CORS_ORIGIN=*

# Rate limiting (per IP)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000

# Session settings
SESSION_TIMEOUT=3600000
